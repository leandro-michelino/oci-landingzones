# Maintainer: Leandro Michelino | ACE | leandro.michelino@oracle.com
---
- name: Run guarded Terraform destroy from a selected directory
  hosts: localhost
  gather_facts: false
  vars:
    terraform_action: destroy
    terraform_backend_enabled: true
  roles:
    - terraform_runner
