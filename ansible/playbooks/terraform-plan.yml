---
- name: Run Terraform plan from a selected directory
  hosts: localhost
  gather_facts: false
  vars:
    terraform_action: plan
    terraform_backend_enabled: false
  roles:
    - terraform_runner
