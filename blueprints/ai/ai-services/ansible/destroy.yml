# Maintainer: Leandro Michelino | ACE | leandro.michelino@oracle.com
---
- name: Destroy OCI AI Services
  hosts: localhost
  gather_facts: false
  vars:
    terraform_action: destroy
    terraform_backend_enabled: false
    terraform_working_dir: "{{ playbook_dir }}/.."
  roles:
    - terraform_runner
