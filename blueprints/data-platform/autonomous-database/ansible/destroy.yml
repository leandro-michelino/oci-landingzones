---
- name: Run Terraform destroy for Autonomous Database
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    terraform_action: destroy
    terraform_backend_enabled: false
    terraform_working_dir: "{{ playbook_dir }}/.."
  roles:
    - terraform_runner
